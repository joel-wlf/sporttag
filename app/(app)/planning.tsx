import { Text, View } from 'react-native';
import { BackofficePage, backofficeStyles as styles, SurfaceCard } from '@/components/backoffice/BackofficePage';
import { palette } from '@/components/ui/theme';
import { useTheme } from '@/hooks/useTheme';
const modules = [['Teams', 'Feste Teams, Nummern, Farben und Größen verwalten.'], ['Spiele & Wertung', 'Vorlagen, Regeln und Tabellenpunkte konfigurieren.'], ['Gelände & Stationen', 'Offline-Karte vorbereiten und feste Standorte setzen.'], ['Zeitplan & Matches', 'Blöcke, Runden, Belegungen und Begegnungen planen.'], ['Betreuung', 'Personen je Station und Block einteilen.'], ['Freigabe', 'Konflikte prüfen und ein Offline-Paket veröffentlichen.']];
export default function PlanningScreen() { const color = palette(useTheme()); return <BackofficePage title="Planung" eyebrow="VERANSTALTUNG VORBEREITEN" description="Alle Bausteine für einen vollständigen und offline nutzbaren Sporttag."><View style={styles.grid}>{modules.map(([title, description], index) => <View key={title} style={styles.cardGrow}><SurfaceCard><View style={[styles.badge, { alignSelf: 'flex-start', backgroundColor: color.accentSoft }]}><Text style={[styles.badgeText, { color: color.accent }]}>{String(index + 1).padStart(2, '0')}</Text></View><Text style={[styles.cardTitle, { color: color.text }]}>{title}</Text><Text style={[styles.cardDescription, { color: color.muted }]}>{description}</Text></SurfaceCard></View>)}</View></BackofficePage>; }
